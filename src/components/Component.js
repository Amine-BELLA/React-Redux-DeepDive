import React from 'react'

function Component() {
    return (
        <div>
            <button>Get State</button>
            <button>Dispatch first action</button>
            <button>Dispatch second action</button>
        </div>
    );
}

export default Component;